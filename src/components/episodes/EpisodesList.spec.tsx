import {} from "@testing-library/react";
describe("EpisodesList", () => {
  it("Should render", () => {
    expect(0).toBe(0);
  });

  it("Should render the episodes list", () => {
    expect(1).toBe(1);
  });
});
