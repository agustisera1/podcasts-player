import {} from "@testing-library/react";
describe("usePodcasts", () => {
  it("Should return the client stored podcasts if not expired and available", () => {
    expect(0).toBe(0);
  });

  it("Should fetch and return podcasts if the storage data expired", () => {
    expect(0).toBe(0);
  });

  it("Should return an empty array and throw a new error if the request fails", () => {
    expect(1).toBe(1);
  });
});
